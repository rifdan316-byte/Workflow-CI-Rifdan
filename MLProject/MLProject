name: Student-Stress-Prediction-Pipeline

python_env: python_env.yaml

entry_points:
  main:
    command: "python modelling.py"
    parameters:
      test_size: {type: float, default: 0.2}
      random_state: {type: float, default: 42}
      model_type: {type: string, default: "all"}
